/*
$(document).ready(function () {
    const originContent = $("#contentMain").text();
    const after = $.parseHTML(originContent);
    $("#contentConvert").append(after);
});

$.ajax({
    type : 'get',
    url :"/board/content",
    dataType : 'html',
    success : function (data) {
        alert(data + "입니다");

    //    $(data).appendTo("#contentConvert");
    }
})
*/

